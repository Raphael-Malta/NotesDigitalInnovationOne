git init (iniciar o repositorio do git)
git add (mover arquivos)
git commit(criar o commit)
Sempre no terminal vc chama primeiro o nome do programa

ao utilizar a flag -a, ela mostra arquivos ocultos
Ou seja  depois de utilizar git init, ele gera uma pasta oculta, e necessita utilizar a flag -a para ver

git config --global user.email "Seu email"
global é pra setar globalmente esses dados

git config --global user.name Seu Nome

MARKDOWN
# (usado em markdown para deixar a fonte maior, pode se usar até 6#)
** palavra ** (deixa em negrito)
_ palavra_ (em italico)
 - espaço (cria uma bolinha preta)
No typora vai em ajuda e dps markdown references q mostra todos os comandos q se pode fazer

